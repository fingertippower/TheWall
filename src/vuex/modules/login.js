const state={

};

const mutations={

};

const actions={
    login:function({}) {
        let p1=document.getElementById('inputPhone').value;
        let p2=document.getElementById('inputPassword').value;
        console.log(p1,p2);
    }
};

const getters={

};

export default {
    state,
    mutations,
    actions,
    getters
}
